# TerraTag

Interactive mining material mapping prototype. Open `index.html` directly, or serve the folder with any static web server.

Features: selectable alteration zones, layer search and visibility, confidence view, zoom controls, material details, and sample tagging.
